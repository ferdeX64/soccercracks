import 'package:flutter/material.dart';

class InternacionalWidget extends StatelessWidget {
  const InternacionalWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
