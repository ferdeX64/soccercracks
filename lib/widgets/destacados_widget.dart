import 'package:flutter/material.dart';

class DestacadosWidget extends StatelessWidget {
  const DestacadosWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
