import 'package:flutter/material.dart';

class NacionalWidget extends StatelessWidget {
  const NacionalWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
